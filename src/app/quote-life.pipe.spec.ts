import { QuoteLifePipe } from './quote-life.pipe';

describe('QuoteLifePipe', () => {
  it('create an instance', () => {
    const pipe = new QuoteLifePipe();
    expect(pipe).toBeTruthy();
  });
});
